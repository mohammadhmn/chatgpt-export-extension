// Shared defaults used by popup + options.
// (Kept as a plain script for MV3 without a build step.)

window.ChatGPTChatExporterDefaults = {
  delayMs: 1500,
  maxChats: 0,
  autoScrollSidebar: true,
  timeoutMs: 45000,
  settleMs: 1500,
  maxSettleWaitMs: 8000
};

